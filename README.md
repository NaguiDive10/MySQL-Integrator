# MySQL-Integrator
MySQL-integrator est un outil d’intégration léger qui envoie des fichiers tabulaires (CSV, Excel) vers une base MySQL, en créant ou remplaçant des tables nommées automatiquement à partir des conventions dimension / fait (dim_*, fact_*).
